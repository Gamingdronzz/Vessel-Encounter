using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class MatchItemTemplate : MonoBehaviour
{
    public TextMeshProUGUI TxtSerial, TxtRoomName, TxtSize, TxtVacancy;

    // Use this for initialization
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}